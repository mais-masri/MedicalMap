<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="?attr/colorSurface">

    <!-- Title for the rating dialog -->
    <TextView
        android:id="@+id/rateplantitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rate Your Plan"
        android:textSize="28dp"
        android:textStyle="bold"
        android:textColor="@color/blue"
        android:layout_gravity="center_horizontal"
        android:paddingBottom="8dp"/>

    <!-- Instruction for the user -->
    <TextView
        android:id="@+id/rateplandescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="How would you rate the plan we suggested for you?"
        android:paddingBottom="8dp"
        android:textSize="23dp"
        android:textColor="@color/blue"/>

    <!-- Spinner for rating 1-10 -->
    <Spinner
        android:id="@+id/planratingspinner"
        android:layout_width="378dp"
        android:layout_height="50dp"
        android:spinnerMode="dropdown" />

    <!-- Buttons for submitting or canceling the rating -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="16dp">



        <!-- Submit Button -->
        <Button
            android:id="@+id/submitbutton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Submit"
            android:layout_marginStart="8dp"
            android:backgroundTint="@color/blue"/>
    </LinearLayout>

</LinearLayout>

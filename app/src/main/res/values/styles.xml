<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Apply custom TextInputLayout style globally -->
        <item name="textInputStyle">@style/CustomTextInputLayout</item>
    </style>

    <!-- Custom AlertDialog style -->
    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">#FFFFFF</item>
        <item name="android:windowMinWidthMajor">80%</item>
        <item name="android:windowMinWidthMinor">80%</item>
    </style>

    <!-- Custom Button Style for Dialog -->
    <style name="DialogButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">#FFFFFF</item> <!-- White text color -->
        <item name="android:backgroundTint">@color/colorPrimary</item> <!-- Primary color background -->
    </style>

    <style name="BoldWeekDayTextAppearance">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item> <!-- Optional: Change text size -->
        <item name="android:textColor">#000000</item> <!-- Optional: Change text color -->
    </style>

    <style name="CalenderViewCustom" parent="Theme.AppCompat">
        <item name="colorControlActivated">@color/blue</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Custom TextInputLayout style for blue frame and hint color -->
    <style name="CustomTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/blue</item> <!-- Frame color for TextInputLayout -->
        <item name="hintTextColor">@color/blue</item>  <!-- Hint color for TextInputLayout -->
        <item name="boxStrokeWidth">2dp</item> <!-- Width of the outline -->
        <item name="boxStrokeWidthFocused">2dp</item> <!-- Width when focused -->
    </style>

</resources>

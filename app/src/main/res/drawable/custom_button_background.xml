<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <stroke android:width="2dp" android:color="@android:color/holo_blue_dark" /> <!-- Blue border -->
    <corners android:radius="8dp" /> <!-- Rounded corners -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="6dp"
        android:bottom="6dp" />
    <solid android:color="@android:color/holo_blue_dark" /> <!-- Blue background -->
</shape>
